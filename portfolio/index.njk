---
layout: default.njk
permalink: /portfolio/
title: 'Portfolio'
eleventyExcludeFromCollections: ["portfolio"]
eleventyNavigation:
  key: Portfolio
  order: 5
---
<div class="container main">
  <div class="row">
    <div class="col-sm-12 typography">
      <div class="page-header">
        <div class="icon">
            <i class="fas fa-briefcase fa-5x"></i>
        </div>
        <div class="description">
            {% markdown %}
            This page contains an overview of my portfolio, including projects or other activities. Below you can find an overview of all my projects. For a list
            of all my publications, please check my [publications page](/publications). For my teaching portfolio, please refer to my [teaching page](/teaching).
            {% endmarkdown %}
        </div>
      </div>
    </div>
  </div>
</div>

<div class="container main">
  <div class="row">
    <div class="col-sm-12 typography">
        <h1>Portfolio</h1>
        <ol reversed class="postlist">
        {% for post in collections.portfolio %}
          {%- set startDate = post.data.start -%}
          {%- set endDate = post.data.end -%}
          <li class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}" itemscope itemtype="http://schema.org/CreativeWork">
            <img class="postlist-image{% if post.data.thumbnail_padding %} padding{% endif %}" alt="{{ post.data.title }}" src="{{ post.data.thumbnail }}" itemprop="image"/>
            <div class="postlist-content">
              <a href="{{ post.url | url }}" class="postlist-link" itemprop="url">{% if post.data.title %}<span itemprop="name">{{ post.data.title }}</span>{% else %}<code>{{ post.url }}</code>{% endif %}</a>
                <span class="postlist-date">{{ startDate | date('MMMM yyyy') }} - {% if post.data.ongoing %}now{% elif endDate %}{{ endDate | date('MMMM yyyy') }}{% endif %}</span>
              <span class="postlist-description" itemprop="description">{{ post.data.excerpt }}</span>
            </div>
          </li>
        {% endfor %}
        </ol>
    </div>
  </div>
</div>