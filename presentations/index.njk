---
layout: default.njk
permalink: /presentations/
title: "Presentations"
eleventyExcludeFromCollections: ["presentations"]
eleventyNavigation:
  key: Talks
  order: 4
---

<div class="container main">
  <div class="row">
    <div class="col-sm-12 typography">
      <div class="page-header">
        <div class="icon">
          <i class="fas fa-chalkboard fa-5x"></i>
        </div>
        <div class="description">
          {% markdown %} Here you can find an overview of all my presentations.
          I cover various topics related to my research and interests. If you
          have any questions or comments, feel free to reach out. {% endmarkdown
          %}
        </div>
      </div>
    </div>
  </div>
</div>

<div class="container main">
  <div class="row">
    <div class="col-sm-12 typography">
      <h1>Presentations</h1>
      <ol reversed class="postlist">
        {% for post in collections.presentations | reverse %}
        <li
          class="postlist-item{% if post.url == url %} postlist-item-active{% endif %}"
          itemscope
          itemtype="http://schema.org/PresentationDigitalDocument"
        >
          <img
            class="postlist-image padding"
            alt="{{ post.data.title }}"
            src="{{ post.data.thumbnail }}"
            itemprop="thumbnailUrl">
          <div class="postlist-content">
            <a href="{{ post.url | url }}" class="postlist-link" itemprop="url"
              >{% if post.data.title %}<span itemprop="name"
                >{{ post.data.title }}</span
              >{% else %}<code>{{ post.url }}</code>{% endif %}</a
            >
            {% if post.data.scheduled %}
            <span class="badge badge-primary">scheduled</span>
            {% endif %}
            <time
              class="postlist-date"
              datetime="{{ post.date | htmlDateString }}"
              itemprop="datePublished"
              >{{ post.date | readableDate }}</time
            >
            <span class="postlist-description" itemprop="description"
              >{{ post.data.excerpt }}</span
            >
          </div>
        </li>
        {% endfor %}
      </ol>
    </div>
  </div>
</div>
